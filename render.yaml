services:
  - type: web
    name: adhello-ai
    runtime: node
    buildCommand: npm run build
    startCommand: node server.js
    plan: free
    region: oregon
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 8080
